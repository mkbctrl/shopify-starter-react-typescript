# Themekit shopify starter with Typescript

The following Shopify theme was created with a Themekit, and enriched with typescript and standalone React component.

## Initial setup
1) Clone repo
2) `yarn install`
